VITE_API_BASE_URL=/api/v1
# 联调模式：false = 经 Gateway 访问后端；true = 本地 Mock（无需后端）
# true 时登录也走 Mock，后端未启动可先进界面（lis/pacs 等页仍需 Gateway 或继续扩 Mock）
VITE_USE_MOCK=false
# 大模型训练组 CT 影像软件地址（跳转，含脑部 CT 进度条等，由对方实现）
# VITE_CT_MODEL_URL=http://localhost:8000/ct-viewer
