---
description: 修改代码前必须征得用户同意；先方案、后实施
alwaysApply: true
---

# 改代码前须用户同意

## 核心原则

未经用户明确同意，**不得**创建、修改或删除项目中的代码与配置文件（含 `.cursor/rules` 自身，除非用户明确要求写入规则）。

## 可直接执行（无需事先征得同意）

- 阅读、搜索、分析代码与日志
- 回答问题、解释逻辑、排查原因
- 给出方案、对比选项、列出建议 diff 要点
- 运行只读命令（如 `git status`、`git diff`、查询 API、读数据库）

## 须先征得同意再执行

- 编辑/新增/删除源码、脚本、配置、SQL、文档（用户未点名要改的文档也包含在内）
- 运行会改变仓库或环境的命令（`git commit`、`npm install`、重启服务等）
- 自动「顺手」重构、格式化、补测试、改无关文件

## 工作流程

1. **先说明**：要改什么、为什么、改哪些文件、预期影响。
2. **等确认**：用户回复「可以」「按你的建议改」「去做」等明确许可后再动手。
3. **再实施**：范围控制在已确认的方案内；若实施中发现需扩大范围，再次说明并等待同意。

## 视为已同意的情形

- 用户在同一条消息里**明确请求**实现某功能或修复（例如：「帮我改 XX」「按方案 A 实现」）。
- 用户明确说「不用问我，直接改」——在该会话内按此执行，除非用户后续收回。

## 禁止

- 未获同意就提交代码、push、改 git config。
- 用「我已经改好了」替代事先沟通（紧急修复也应先简短说明再改，除非用户消息本身就是实施指令）。
